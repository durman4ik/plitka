.field.delete-article-image
  - @images.each do |image|
    = image_tag image.url, width: '200'
    = link_to 'Удалить', album_image_path(image.album_id, image), method: :delete, remote: true

    %br